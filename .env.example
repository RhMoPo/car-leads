# Database Configuration
DATABASE_URL=postgresql://postgres:secure_password_123@localhost:5432/car_leads
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=secure_password_123
PGDATABASE=car_leads

# Application Configuration
NODE_ENV=production
PORT=5000
SESSION_SECRET=your_very_long_random_session_secret_key_here_at_least_32_characters

# Admin Authentication
# Generate with: node -e "console.log(require('bcrypt').hashSync('your_admin_password', 10))"
ADMIN_PASSWORD_HASH=$2b$10$example_hash_replace_with_your_actual_bcrypt_hash

# Optional: Email Configuration (for future notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: File Storage (for future file uploads)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=car-leads-uploads